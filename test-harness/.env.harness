# Suppress verbose Pino INFO logs during harness runs so terminal scrollback
# doesn't overflow (664 assertion lines + hundreds of INFO messages = too much).
# Only WARN and above will appear in the terminal.
# Change to "debug" if you need full trace output.
ARGUS_LOG_LEVEL=warn
